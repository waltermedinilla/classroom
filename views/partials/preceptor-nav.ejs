<%# preceptor_dashboard está locked en config/sections.js (es la única solapa del panel:
    apagarla equivaldría a borrarle el rol a alguien), así que hoy esto siempre da true.
    Va igual para que el día que el panel tenga más solapas el patrón ya esté puesto. %>
<% const puede = (k) => (typeof can === 'function' ? can(k) : true); %>
<nav class="admin-nav">
  <% if (puede('preceptor_dashboard')) { %>
  <a href="/preceptor" class="admin-nav-item <%= activePage === 'dashboard' ? 'active' : '' %>">
    <span class="material-symbols-outlined">school</span>
    Mis cursos
  </a>
  <% } %>
</nav>
